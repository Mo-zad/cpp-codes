#include <iostream>
using namespace std;


class list {
    private:
    protected:
    public:
    list(){ 
        //constructor
    }
    ~list(){
        //destructor
    }
    float calc_area(float width, float length);
    float calc_perimeter(float width, float length);
};